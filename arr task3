#include<stdio.h>
int main()
{
    int n;
    printf("enter a array size\n");
    scanf("%d",&n);
    int arr[n];
    printf("enter the array elements\n");
    for(int i==0;i<n;i++)
    {
        scanf("%d\n",arr[i]);
    }
    printf("The array elements are \n");
    for (int i=0;i<n;i++)
    {
        printf("%d\n",arr[i]);
    }
    return 0;
}
