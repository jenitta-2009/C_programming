#include <stdio.h>

int main()
{
   int arr[5];
   int i,smallest;
   
   
   printf("Enter any 5 numbers\n");
   for(int i=0;i<5;i++)
   {
       scanf("%d",&arr[i]);
       
   }
   int m=arr[0];
   for(int j=1;j<5;j++)
   {
       if(arr[j]<m)
       {
           m=arr[j];//48
       }
   }
   printf("smallest elements in a array %d",m);
   
    return 0;
}
