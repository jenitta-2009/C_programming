/*
//armstrong no
#include <stdio.h>
#include <math.h>

int main() 
{
    int a;
    printf("Enter a No");
    scanf("%d",&a);
    int r,c=0,s=0;
    int t=a;
    while(a>0)
    {
        a/=10;
        c++;
    }
    a=t;
    while(a>0)
    {
        r=a%10;
        s+=pow(r,c);
        a/=10;
    }
    if (t==s)
    {
        printf("armstrong number");
    }
    else
    {
        printf("not a armstrong number");
    }

    return 0;
}
*/
